dist/
Resources/

# Per-machine Claude Code settings. Records the literal Bash commands that were
# approved, so it leaks local paths and whatever was poked at while debugging.
.claude/settings.local.json
